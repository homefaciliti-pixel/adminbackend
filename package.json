{
  "name": "admin_panel_monorepo",
  "version": "1.0.0",
  "description": "Root package.json to delegate build and start commands to the backend folder",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "cd backend && npm start",
    "build": "cd backend && npm install"
  },
  "dependencies": {
    "mysql2": "^3.23.3",
    "pdfkit": "^0.19.1"
  }
}
